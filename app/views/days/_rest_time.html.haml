= form_with model: exercise,  id: dom_id(exercise) do |exercise|
  = exercise.checkbox :copy_rest_time
  = exercise.text_field :rest
  = exercise.submit 'Update', class: 'btn btn-primary'
= countdown exercise.rest
