%p{:style => "color: green"}= notice
= render @schedule
%div
  = link_to "Edit this schedule", edit_schedule_path(@schedule)
  |
  \#{link_to "Back to schedules", schedules_path}

  \#{button_to "Destroy this schedule", @schedule, method: :delete}
  Weeks:
  - @schedule.weeks.each do |week|
    %div
      = link_to("Week: #{week.number} - #{week.week_status.name}", week_path(week))
      = ": #{week.days.size} days"
      = link_to 'Duplicate', duplicate_week_path(id: week), data: { turbo_method: :post }
      \#{button_to 'Destroy Week', week, method: :delete}