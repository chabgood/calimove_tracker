<h1>Upload a New Schedule</h1>

<%= form_with(model: @schedule, multipart: true, local: true) do |form| %>
  <div class="field">
    <%= form.label :file %>
    <%= form.file_field :file %>
  </div>

  <div class="actions">
    <%= form.submit 'Upload' %>
  </div>
<% end %>
